const user = (state = [], action) => {
  switch(action.type) {
    default:
      return state;
  }
};

const users = (state = [], action) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default users;
